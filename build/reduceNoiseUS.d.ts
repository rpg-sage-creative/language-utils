/** Removes words considered to be "noise" words. */
export declare function reduceNoiseUS(terms: string[]): string[];
/** Removes words considered to be "noise" words. */
export declare function reduceNoiseUS(terms: string[], additionalNoise: string[]): string[];
